The goal is to port biomodals to work with lyceum.

Create a projects/biolyceum directory.

The code for biolyceum will live in projects/biolyceum/src

Make a plan for which tasks need to be completed to port biomodals

Some ideas:

1. Read lyceum docs and understand differences to modal; write plan for port
2. Check lyceum access (api/user key or similar)
3. Rewrite biomodals utils
4. Rewrite a single simple /lightweight biomodals entrypoint
5. Rewrite remaining entrypoints (one-by-one maybe, asking for the next each time)\

Write a <task_number>-<task_name>.md file for each of the tasks that you foresee having to complete.

Write a /bioltask skill for running a given task.